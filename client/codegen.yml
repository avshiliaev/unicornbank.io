schema: http://localhost:8080/graphql
documents: ./src/api/schema/documents/**/*.gql
generates:
  ./src/api/interfaces/types.d.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
